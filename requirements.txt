# 로컬 PC(Unity + 웹 콘솔이 도는 쪽)에서 순찰 파이프라인을 돌릴 때의 의존성.
#
#   python -m venv .venv && source .venv/bin/activate     # conda여도 무방
#   pip install -r requirements.txt
#   python api_server.py --llm-url http://<GPU서버>:8000/v1
#
# **torch도 transformers도 없다.** LLM은 GPU 서버의 llm_server/ 가 들고 있고
# 이쪽은 patrol/remote_llm.py 가 HTTP로 물어보기만 한다. 그 클라이언트는 표준
# 라이브러리(urllib)만 쓰므로 requests/openai 도 필요 없다.
# 3D 인식도 사전계산 결과(data/final_npy)를 읽기만 해서
# spconv/MinkowskiEngine/CLIP이 하나도 필요 없다.
#
# GPU 서버 쪽은 llm_server/requirements.txt 를 쓴다.
#
# 사람 탐지(simulator/bridge/person_detector_tcp.py)에 필요한 ultralytics 는
# **일부러 여기 없다** — torch 를 끌고 오기 때문이다. 그건 Unity 가 TCP 9100 으로
# 부르는 별도 프로세스라 이 목록과 같은 환경에 있을 필요가 없다. 쓸 때만:
#   python -m pip install ultralytics

numpy>=1.24                # numpy 2로 검증 완료 — 제거된 API를 쓰지 않는다

# 웹 콘솔에 붙는 API 서버 (api_server.py). 자동 /docs 가 프론트 규격서 역할을 한다.
fastapi>=0.110
uvicorn>=0.29

# 아래 둘은 없어도 동작한다 (각각 numpy 폴백 / 임베드 생략). 있는 쪽이 빠르고 낫다.
scipy>=1.10                # planner.py 장애물 팽창 (없으면 느린 numpy roll 폴백)
pillow>=10.0               # patrol_report.py 사진 축소 임베드 (없으면 링크만)
