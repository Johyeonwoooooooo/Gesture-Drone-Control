# Mosaic3D inference-only deps (skip MinkowskiEngine/pointops/cuml which are training/seg3d only)
# Install order matters: torch first, then torch-scatter/cluster against the matching torch wheel.

# --- PyTorch (cu121) ---
torch==2.2.2
torchvision==0.17.2

# --- Lightning / Hydra ---
lightning==2.2.5
torchmetrics>=0.11.4
hydra-core==1.3.2
hydra-colorlog==1.2.0
omegaconf
rootutils
rich
fire
overrides

# --- SparseUNet backbone ---
spconv-cu120

# --- Geometry I/O ---
numpy<2
scipy
plyfile
trimesh
open3d==0.18.0
einops
natsort
easydict
pyyaml
tqdm
imageio
matplotlib
scikit-image

# --- CLIP text ---
# transformers 5.x dropped support for torch<2.4; pin to 4.x
transformers<5
open_clip_torch
clip @ git+https://github.com/openai/CLIP.git
ftfy
regex

# Lightning 2.2 still imports pkg_resources at startup; setuptools>=81 removed it.
setuptools<81

# --- torch-scatter/cluster for some pointcept ops ---
torch-scatter
torch-cluster
jaxtyping

# --- Webapp ---
viser
fastapi
uvicorn

# --- Misc ---
gdown
loguru
