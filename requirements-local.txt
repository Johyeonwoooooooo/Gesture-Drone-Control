# 노트북(Unity가 도는 쪽)에서 파이프라인을 돌릴 때의 의존성.
#
#   python -m venv .venv && source .venv/bin/activate     # conda여도 무방
#   pip install -r requirements-local.txt
#   python patrol/server.py --sim --unity-host 127.0.0.1 \
#       --llm-url http://166.104.223.32:8000/v1
#
# torch도 transformers도 없다. LLM은 GPU 서버의 patrol/llm_serve.py 가 들고
# 있고, 이쪽은 patrol/remote_llm.py 가 HTTP로 물어보기만 한다. 그 클라이언트는
# 표준 라이브러리(urllib)만 쓰므로 requests/openai 도 필요 없다.
#
# GPU 서버 쪽(=LLM을 직접 로드하는 쪽)은 requirements.txt 를 쓴다.

numpy>=1.24

# 아래 둘은 없어도 동작한다 (각각 numpy 폴백 / 임베드 생략). 있는 쪽이 빠르고 낫다.
scipy>=1.10                # planner.py 장애물 팽창 (없으면 느린 numpy roll 폴백)
pillow>=10.0               # patrol_report.py 사진 축소 임베드 (없으면 링크만)
